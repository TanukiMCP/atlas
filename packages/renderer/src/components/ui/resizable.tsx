import { Panel, PanelGroup, PanelResizeHandle } from "react-resizable-panels";

export const ResizablePanelGroup = PanelGroup;
export const ResizablePanel = Panel;
export const ResizableHandle = PanelResizeHandle;